<p>
  <strong>Title:</strong>
  <%= @note.title %>
</p>

<p>
  <strong>Content:</strong>
  <%= @note.content %>
</p>

<% if user_signed_in? %>
  <%= link_to 'Edit', edit_note_path(@note) %> |
<% end %>

<%= link_to 'Back', notes_path %>